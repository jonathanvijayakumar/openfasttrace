package org.itsallcode.openfasttrace.core;

/**
 * The tracing mode decides how strict the tracer evaluates the specification
 * item status when calculating the trace.
 */
public enum TraceMode {
	STRICT, RELAXED
}